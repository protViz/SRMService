context("test-renderReports")
library(SRMService)
tmp_d <- tempdir()

packagedir <- path.package("SRMService")

test_that("source Run Tidy analysis", {
  #reportFile <- file.path(packagedir,
  #                        "inst",
  #                        "RunScripts",
  #                        "Run_TidyAnalysis_Skyline_PRM.R")
  # source(reportFile)
  })

